# pip fallback if you are not using environment.yml.
# For GPU (RTX 3060): pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu124
torch
torchvision
timm
pandas
pillow
pyyaml
tqdm
numpy
